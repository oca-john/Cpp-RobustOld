#include <stdio.h>

// ternary operator, sanyuan yunsuan

int main()
{
	int x = 20;	// define and int x as 20;
	char y;		// define y as char;
	y = (x == 2 ? 100 : 40);	// check whether x is 2, if yes y is 100, or y is 40;
	printf("y value is %d\n",y);
	return 0;
}

