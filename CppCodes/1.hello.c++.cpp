#include <iostream>
// declare the "i/o stream lib" in the whole source file
// like #include <stdio.n> in C lang

int main() {        // each cpp file has only one main() function
    std::cout << "hello world!" << std::endl;
    return 0;       // the main() function must return a result to system
}
